<Window x:Class="NetSupport.Tutor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetSupport Tutor - 2026 Edition" Height="650" Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="#F4F7FE" FontFamily="Segoe UI">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!-- Modern Rounded Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect Color="#4F46E5" BlurRadius="10" Opacity="0.3" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4338CA" TargetName="border"/>
                                <Setter Property="Effect" TargetName="border">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#4338CA" BlurRadius="15" Opacity="0.5" ShadowDepth="3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3730A3" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#4F46E5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderBrush="#E0E7FF" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="#94A3B8" BlurRadius="5" Opacity="0.1" ShadowDepth="1"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect Color="#EF4444" BlurRadius="10" Opacity="0.3" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DC2626" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#EEF2FF"/>
                        <Setter Property="Foreground" Value="#3730A3"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center" Margin="10,0"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header Navigation Bar -->
        <Border Grid.Row="0" Background="White" CornerRadius="15" Padding="15,10" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#94A3B8" BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Image Source="C:\Users\Assem_CS\.gemini\antigravity\brain\1b56644b-a412-4142-a388-433e9e8b8a73\netsupport_tutor_logo_1777514418491.png" Width="40" Height="40" Margin="0,0,15,0"/>
                    <TextBlock Text="NetSupport Tutor" FontSize="22" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    <TextBlock Text="2026 Edition" FontSize="14" FontWeight="SemiBold" Foreground="#4F46E5" VerticalAlignment="Center" Margin="10,4,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Label x:Name="LblLanguage" Content="Language:" FontWeight="SemiBold" Foreground="#64748B" VerticalAlignment="Center"/>
                    <ComboBox x:Name="LanguageToggle" Width="100" Height="35" Margin="5,0,0,0" SelectionChanged="LanguageToggle_SelectionChanged" VerticalContentAlignment="Center" Background="#F8FAFC" BorderBrush="#E2E8F0">
                        <ComboBoxItem Content="English" IsSelected="True" Tag="en"/>
                        <ComboBoxItem Content="العربية" Tag="ar"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Action Panel -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#94A3B8" BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="LockBtn" Content="Lock Selected" Width="130" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="LockBtn_Click"/>
                    <Button x:Name="UnlockBtn" Content="Unlock Selected" Width="130" Margin="0,0,10,0" Style="{StaticResource SecondaryButton}" Click="UnlockBtn_Click"/>
                    <Button x:Name="ClearHelpBtn" Content="Clear Help" Width="130" Margin="0,0,10,0" Style="{StaticResource SecondaryButton}" Click="ClearHelpBtn_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="TestingConsoleBtn" Content="Open Testing Console" Width="160" Margin="0,0,10,0" Style="{StaticResource SecondaryButton}" Click="TestingConsoleBtn_Click"/>
                    <Button x:Name="StartExamBtn" Content="Start Exam" Width="120" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Background="#10B981" Click="StartExamBtn_Click"/>
                    <Button x:Name="StopExamBtn" Content="Stop Exam" Width="120" Margin="0,0,10,0" Style="{StaticResource DangerButton}" Click="StopExamBtn_Click"/>
                    <Button x:Name="AnalyticsBtn" Content="Live Analytics" Width="130" Margin="0,0,10,0" Style="{StaticResource ModernButton}" Background="#8B5CF6" Click="AnalyticsBtn_Click"/>
                    <Button x:Name="GenerateReportBtn" Content="Generate Report" Width="140" Margin="0,0,0,0" Style="{StaticResource ModernButton}" Background="#06B6D4" Click="GenerateReportBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Data Grid Area -->
        <Border Grid.Row="2" Background="White" CornerRadius="15" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="#94A3B8" BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="LblTitle" Text="Discovered Students" FontWeight="Bold" FontSize="18" Foreground="#1E293B" Margin="0,0,0,15"/>
                
                <DataGrid x:Name="StudentsDataGrid" Grid.Row="1" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Help" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="✋" Foreground="Red" FontSize="20" HorizontalAlignment="Center" Visibility="{Binding IsRaisingHand, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Student Name" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="IP Address" Binding="{Binding Ip}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding IsReady}" Width="80" />
                        <DataGridTextColumn Header="Locked" Binding="{Binding IsLocked}" Width="80" />
                        <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
