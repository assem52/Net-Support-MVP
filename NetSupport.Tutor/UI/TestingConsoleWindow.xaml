<Window x:Class="NetSupport.Tutor.UI.TestingConsoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Testing Console" Height="300" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Exam Assignment" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BtnBrowse" Content="Browse CSV..." Width="100" Click="BtnBrowse_Click"/>
            <TextBlock x:Name="TxtFileName" Text="No file selected" Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Duration (minutes):" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="TxtDuration" Width="50" Text="30" VerticalContentAlignment="Center"/>
        </StackPanel>

        <Button x:Name="BtnPush" Grid.Row="3" Content="Push Exam to Selected Student" Height="40" VerticalAlignment="Top" Click="BtnPush_Click"/>
    </Grid>
</Window>
